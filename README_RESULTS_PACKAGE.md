# Repository additions from the production runs

This package contains compact merged outputs, derived tables, run summaries,
and generated configuration files from the completed simulation campaign.

The package deliberately excludes raw dense trajectories, videos, rendered
media, job logs, Slurm files, and other large cluster-specific outputs.
